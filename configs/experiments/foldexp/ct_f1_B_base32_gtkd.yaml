# ARM B = base32, GT + KD (distill_w 0.5) from the FOLD-CLEAN base48 teacher (ARM A). = the standard
# distillation recipe (same as the deployed base40/base32 students, but fold-clean so held-out is valid).
# The reference "distilled small model" that C (scratch) and D (init+ft) are compared against.
# DEPENDS ON ARM A: teacher_ckpt = runs/foldexp_ct_f1_A_base48/state.pt (must finish first).
exp_name: foldexp_ct_f1_B_base32gtkd
fold: 1
modality: ct
in_ch: 6
base_ch: 32
levels: 4
bottleneck: dilated
distill_w: 0.5
teacher_ckpt: /home/kaiwang/doserad2026_workdir/runs/foldexp_ct_f1_A_base48/state.pt
add_naive: true
augment: false
naive_skin_gate: true
patch: [128, 128, 128]
batch_size: 2
fg_prob: 0.7
hi_frac: 0.1
hi_w: 10.0
grad_w: 3.0
het_w: 4.0
lung_w: 2.0
lung_lo: 0.02
lung_hi: 0.6
max_steps: 120000
lr: 2.0e-04
weight_decay: 0.0001
ckpt_every: 20000
log_every: 200
cache_dir: /home/kaiwang/doserad2026_workdir/cache/crops/photon_skinentry_m24
splits: /home/kaiwang/doserad2026_workdir/splits_final.json
run_root: /home/kaiwang/doserad2026_workdir/runs
wandb: true
wandb_project: doserad2026
