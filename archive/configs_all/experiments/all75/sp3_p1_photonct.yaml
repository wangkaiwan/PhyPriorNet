# 3mm-RESOLUTION photon-CT full-data training, stage 1 (user 2026-08-25: "直接上全数据训练, 超参数
# 和之前的全数据训练一致, 只训练一次然后上传"). EXACT copy of m24_p1_photonct (the deployed model's
# from-scratch stage) except: cache -> photon_skinentry_3mm_m16 (3mm grid, margin 16vox=48mm = same
# PHYSICAL margin as m24@2mm) and dose_spacing_mm 3.0 (inert at gamma_w 0). patch [128,128,128] kept
# NUMERICALLY identical (crops < patch already happened at 2mm; dataset pads the same way).
# Purpose: photon runtime is 85% net-forward; 3mm = 0.30x voxels -> ~2-3x per-CP -> board sim ~40s =
# Mean ~10 = #1 IF gamma holds. Deploy: predict at 3mm, upsample to the 2mm source grid.
exp_name: sp3_p1_photonct
fold: all
modality: ct
in_ch: 6
add_naive: true
bottleneck: dilated
base_ch: 48
augment: false
grad_w: 3.0
het_w: 4.0
lung_w: 2.0
lung_lo: 0.02
lung_hi: 0.6
patch:
- 128
- 128
- 128
batch_size: 2
fg_prob: 0.7
max_steps: 200000
lr: 0.0003
weight_decay: 0.0001
hi_frac: 0.1
hi_w: 10.0
gamma_w: 0.0
dose_spacing_mm: 3.0
ckpt_every: 5000
log_every: 200
val_every: 10000
val_crops: 240
val_gamma_crops: 0
levels: 4
num_workers: 8
wandb: true
wandb_project: doserad2026
cache_dir: /home/kaiwang/doserad2026_workdir/cache/crops/photon_skinentry_3mm_m16
splits: /home/kaiwang/doserad2026_workdir/splits_all75.json
run_root: /home/kaiwang/doserad2026_workdir/runs
snap_every: 5000
