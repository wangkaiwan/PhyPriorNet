exp_name: v9abl_nograd_photon_ct
fold: 0
modality: ct
# v9 WEIGHTING ABLATION (2026-06-14, from-scratch, MATCHED 80k, same as v9 EXCEPT the 3 loss
# weights below). Isolates which of penumbra(grad)/interface(het)/low-density(lung) drives v9's
# gain. Prior held FIXED at v9's naive (in_ch=6) so only the WEIGHTING varies. Result updates the
# v13 arch design (drop grad_w if harmful). het+lung, NO penumbra -> decides v13's grad_w.
in_ch: 6
add_naive: true
augment: false
grad_w: 0.0
het_w: 4.0
lung_w: 2.0
lung_lo: 0.02
lung_hi: 0.6
patch: [128, 128, 128]
batch_size: 2
fg_prob: 0.7
max_steps: 80000
lr: 3.0e-4
weight_decay: 1.0e-4
hi_frac: 0.1
hi_w: 10.0
gamma_w: 0.0
dose_spacing_mm: 2.0
ckpt_every: 5000
log_every: 200
val_every: 10000
val_crops: 240
val_gamma_crops: 0
base_ch: 48
levels: 4
num_workers: 4
wandb: true
wandb_project: doserad2026
cache_dir: /home/kaiwang/doserad2026_workdir/cache/crops/photon
splits: /home/kaiwang/doserad2026_workdir/splits_final.json
run_root: /home/kaiwang/doserad2026_workdir/runs
